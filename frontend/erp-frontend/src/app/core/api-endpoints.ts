/** Central API paths (relative to `environment.apiBaseUrl`). */
export const ApiEndpoints = {
  items: '/api/items',
  locations: '/api/locations',
  purchaseBills: '/api/purchase-bill',
  auditLogs: '/api/audit-logs',
} as const;
